# ProjectMatch Environment Configuration
PORT=5000

# Optional: Google Gemini API Key for dynamic LLM match explainability
# If omitted or offline, the system automatically uses the resilient local template engine.
GEMINI_API_KEY=
